[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# GTNH server takes a while to start (world gen, mod loading)
healthcheckPath = ""
healthcheckTimeout = 600
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
